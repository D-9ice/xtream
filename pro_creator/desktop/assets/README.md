# Desktop Icon Assets

Place the provided app icon files here:

- `icon.png` (1024x1024 PNG)
- `icon.icns` (macOS)
- `icon.ico` (Windows)

These are referenced in `desktop/package.json` for packaging.
